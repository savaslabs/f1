if testing == undefined
  - const testing = true
- const siteTitle = "The Next F1 Race"
- const siteURL = "https://thenextf1race.com"
- const description = "Stay on track anywhere you go with with The Next F1 Race, the ultimate destination for the Formula 1 racing schedule. It's the prettiest race schedule on the internet."
- const d = new Date()
- const lastUpdated = d.getFullYear() + '-' + (d.getMonth()<10?'0':'') + d.getMonth() + '-' + (d.getDate()<10?'0':'') + d.getDate() + '@' + (d.getHours()<10?'0':'') + d.getHours() + ':' + (d.getMinutes()<10?'0':'') + d.getMinutes() + ':' + (d.getSeconds()<10?'0':'') + d.getSeconds()